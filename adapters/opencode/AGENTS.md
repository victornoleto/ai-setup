# OpenCode Adapter

The canonical global instructions and portable memory protocol are loaded from
`/home/victor/.ai-setup/sync/memory/` through `opencode.jsonc`.

If a project contains `.ai/README.md`, read it before working. Its project-specific instructions
override generic guidance.
